# Enable the Ztest unit testing framework
CONFIG_ZTEST=y

# Randomize suite and test order to catch ordering dependencies early
CONFIG_ZTEST_SHUFFLE=y

# Enable logging
# for unit_testing
CONFIG_LOG=y

# Enable LOG in ring_buf
CONFIG_RING_BUF_LOG=y
